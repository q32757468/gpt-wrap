{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "about",
  "description": "Allows the native GPTWrap about window to close itself and open its project homepage.",
  "windows": ["about"],
  "permissions": [
    "core:window:allow-close",
    "opener:default"
  ]
}
